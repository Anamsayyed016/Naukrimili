name: Deploy to Production (Zero-Downtime)

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  HOST: ${{ secrets.HOST }}
  SSH_USER: ${{ secrets.SSH_USER }}
  SSH_KEY: ${{ secrets.SSH_KEY }}
  SSH_PORT: ${{ secrets.SSH_PORT }}
  NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
  DATABASE_URL: ${{ secrets.DATABASE_URL }}
  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
  GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
  GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
  GOOGLE_CLOUD_OCR_API_KEY: ${{ secrets.GOOGLE_CLOUD_OCR_API_KEY }}
  PROD_URL: https://naukrimili.com
  DEPLOY_FOLDER: /var/www/naukrimili
  STAGING_FOLDER: /var/www/naukrimili-staging
  BACKUP_FOLDER: /var/www/naukrimili-backup

jobs:
  validate_secrets:
    name: "🔐 Validate Secrets"
    runs-on: ubuntu-latest
    steps:
      - name: Validate All Required Secrets
        run: |
          set +e
          echo "════════════════════════════════════════════════════════════"
          echo "🔐 VALIDATING ALL PRODUCTION SECRETS"
          echo "════════════════════════════════════════════════════════════"
          echo ""
          
          MISSING=0
          
          # Check critical deployment secrets
          [ -z "$HOST" ] && echo "❌ HOST: MISSING" && MISSING=1 || echo "✅ HOST: SET (${#HOST} chars)"
          [ -z "$SSH_USER" ] && echo "❌ SSH_USER: MISSING" && MISSING=1 || echo "✅ SSH_USER: SET"
          [ -z "$SSH_PORT" ] && echo "❌ SSH_PORT: MISSING" && MISSING=1 || echo "✅ SSH_PORT: SET"
          [ -z "$SSH_KEY" ] && echo "❌ SSH_KEY: MISSING" && MISSING=1 || echo "✅ SSH_KEY: SET (${#SSH_KEY} chars)"
          [ -z "$NEXTAUTH_SECRET" ] && echo "❌ NEXTAUTH_SECRET: MISSING" && MISSING=1 || {
            if [ ${#NEXTAUTH_SECRET} -lt 32 ]; then
              echo "❌ NEXTAUTH_SECRET: TOO SHORT (${#NEXTAUTH_SECRET} < 32 chars)"
              MISSING=1
            else
              echo "✅ NEXTAUTH_SECRET: SET (${#NEXTAUTH_SECRET} chars)"
            fi
          }
          [ -z "$DATABASE_URL" ] && echo "❌ DATABASE_URL: MISSING" && MISSING=1 || echo "✅ DATABASE_URL: SET"
          
          # Check optional keys (AI APIs - not required for deployment)
          [ -z "$OPENAI_API_KEY" ] && echo "⚠️  OPENAI_API_KEY: NOT SET (optional)" || echo "✅ OPENAI_API_KEY: SET"
          [ -z "$GEMINI_API_KEY" ] && echo "⚠️  GEMINI_API_KEY: NOT SET (optional)" || echo "✅ GEMINI_API_KEY: SET"
          
          echo ""
          if [ $MISSING -eq 1 ]; then
            echo "════════════════════════════════════════════════════════════"
            echo "❌ DEPLOYMENT BLOCKED: Missing secrets!"
            echo "════════════════════════════════════════════════════════════"
            echo ""
            echo "📋 SETUP INSTRUCTIONS:"
            echo "1. Go to: https://github.com/Anamsayyed016/Naukrimili/settings/secrets/actions"
            echo "2. Add these secrets:"
            echo ""
            echo "   HOST = srv1054971.hstgr.cloud"
            echo "   SSH_USER = root"
            echo "   SSH_PORT = 22"
            echo "   SSH_KEY = (paste complete private key)"
            echo "   NEXTAUTH_SECRET = (32+ random characters)"
            echo "   DATABASE_URL = postgresql://user:pass@host:5432/db"
            echo ""
            echo "   Note: Google OAuth has been removed. Using manual authentication only."
            echo ""
            exit 1
          fi
          
          echo "✅ ALL SECRETS VALIDATED"
          echo "════════════════════════════════════════════════════════════"

  build:
    name: "🔨 Build Application"
    runs-on: ubuntu-latest
    needs: validate_secrets
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Verify and fix package-lock.json
        run: |
          # Check if package-lock.json exists
          if [ ! -f package-lock.json ]; then
            echo "⚠️  package-lock.json not found, generating it..."
            npm install --package-lock-only --legacy-peer-deps || {
              echo "❌ Failed to generate package-lock.json"
              exit 1
            }
            echo "✅ package-lock.json generated"
          else
            # Check for Prisma version mismatch
            echo "🔍 Checking for Prisma version mismatches..."
            PACKAGE_JSON_PRISMA=$(grep -o '"@prisma/client": "[^"]*"' package.json | cut -d'"' -f4)
            PACKAGE_JSON_PRISMA_CLI=$(grep -o '"prisma": "[^"]*"' package.json | cut -d'"' -f4)
            LOCK_PRISMA=$(grep -o '"@prisma/client": "[^"]*"' package-lock.json | head -1 | cut -d'"' -f4)
            LOCK_PRISMA_CLI=$(grep -o '"prisma": "[^"]*"' package-lock.json | head -1 | cut -d'"' -f4)
            
            echo "   package.json: @prisma/client=$PACKAGE_JSON_PRISMA, prisma=$PACKAGE_JSON_PRISMA_CLI"
            echo "   package-lock.json: @prisma/client=$LOCK_PRISMA, prisma=$LOCK_PRISMA_CLI"
            
            # Check if versions don't match (allowing for ^ prefix)
            MISMATCH=0
            if [ -n "$PACKAGE_JSON_PRISMA" ] && [ -n "$LOCK_PRISMA" ]; then
              # Remove ^ prefix for comparison
              PKG_VERSION=$(echo "$PACKAGE_JSON_PRISMA" | sed 's/^[^0-9]*//')
              LOCK_VERSION=$(echo "$LOCK_PRISMA" | sed 's/^[^0-9]*//')
              if [ "$PKG_VERSION" != "$LOCK_VERSION" ]; then
                echo "   ❌ @prisma/client version mismatch: package.json=$PKG_VERSION, lock=$LOCK_VERSION"
                MISMATCH=1
              fi
            fi
            
            if [ -n "$PACKAGE_JSON_PRISMA_CLI" ] && [ -n "$LOCK_PRISMA_CLI" ]; then
              PKG_CLI_VERSION=$(echo "$PACKAGE_JSON_PRISMA_CLI" | sed 's/^[^0-9]*//')
              LOCK_CLI_VERSION=$(echo "$LOCK_PRISMA_CLI" | sed 's/^[^0-9]*//')
              if [ "$PKG_CLI_VERSION" != "$LOCK_CLI_VERSION" ]; then
                echo "   ❌ prisma CLI version mismatch: package.json=$PKG_CLI_VERSION, lock=$LOCK_CLI_VERSION"
                MISMATCH=1
              fi
            fi
            
            if [ $MISMATCH -eq 1 ]; then
              echo "⚠️  Version mismatch detected! Regenerating package-lock.json..."
              rm -f package-lock.json
              # Install Prisma 6.18.0 first to ensure it's in the lock file
              npm install prisma@6.18.0 @prisma/client@6.18.0 --save-dev --save --legacy-peer-deps --package-lock-only || {
                echo "❌ Failed to regenerate package-lock.json with Prisma 6.18.0"
                exit 1
              }
              echo "✅ package-lock.json regenerated with Prisma 6.18.0"
            else
              echo "✅ package-lock.json versions match package.json"
            fi
          fi

      - name: Install dependencies
        env:
          SKIP_POSTINSTALL: true
        run: |
          npm config set registry https://registry.npmjs.org/
          npm config set progress false
          npm config set audit false
          npm config set fund false
          echo "📦 Installing production dependencies..."
          # Try npm ci first (faster, more reliable if lock file is good)
          npm ci --omit=dev --legacy-peer-deps --ignore-scripts 2>&1 | tee npm-install.log || {
            echo "⚠️  npm ci failed, checking error..."
            if grep -q "package-lock.json" npm-install.log; then
              echo "📝 Lock file issue detected, regenerating..."
              rm -f package-lock.json
              npm install --package-lock-only --legacy-peer-deps
              npm ci --omit=dev --legacy-peer-deps --ignore-scripts || {
                echo "❌ npm ci failed after lock file regeneration"
                cat npm-install.log
                exit 1
              }
            else
              echo "❌ npm ci failed with unknown error:"
              cat npm-install.log
              exit 1
            fi
          }
          [ -d node_modules ] || { echo "❌ node_modules not created"; exit 1; }
          echo "✅ Production dependencies installed"
          
      - name: Verify dotenv is installed
        run: |
          echo "🔍 Verifying dotenv is installed..."
          if [ ! -d "node_modules/dotenv" ]; then
            echo "⚠️  dotenv not found, installing..."
            npm install dotenv@16.4.5 --save --legacy-peer-deps --no-save || {
              echo "❌ Failed to install dotenv"
              exit 1
            }
          fi
          [ -d "node_modules/dotenv" ] || { echo "❌ dotenv still not found after install"; exit 1; }
          echo "✅ dotenv verified/installed"

      - name: Force install Prisma 6.18.0 (remove any 7.x versions)
        run: |
          echo "📦 Forcing Prisma 6.18.0 installation..."
          
          # Remove any existing Prisma 7.x installations
          echo "🧹 Removing any Prisma 7.x installations..."
          npm uninstall prisma @prisma/client @prisma/client-runtime-utils --legacy-peer-deps 2>/dev/null || true
          rm -rf node_modules/.prisma node_modules/@prisma node_modules/.bin/prisma 2>/dev/null || true
          
          # Install Prisma 6.18.0 explicitly
          echo "📦 Installing Prisma 6.18.0 (CLI and Client)..."
          npm install prisma@6.18.0 @prisma/client@6.18.0 --save-dev --save --legacy-peer-deps --no-save --force || {
            echo "❌ Failed to install Prisma 6.18.0"
            exit 1
          }
          
          # Verify installation
          [ -f node_modules/.bin/prisma ] || { echo "❌ Prisma binary not found"; exit 1; }
          [ -d node_modules/@prisma/client ] || { echo "❌ @prisma/client not found"; exit 1; }
          
          # Verify versions are 6.18.0
          PRISMA_VERSION=$(./node_modules/.bin/prisma --version 2>/dev/null | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1 || echo "")
          CLIENT_VERSION=$(npm list @prisma/client --depth=0 2>/dev/null | grep "@prisma/client" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1 || echo "")
          
          echo "   Prisma CLI version: $PRISMA_VERSION"
          echo "   @prisma/client version: $CLIENT_VERSION"
          
          # Ensure both are exactly 6.18.0 (or at least 6.x)
          if [ -z "$PRISMA_VERSION" ] || [ -z "$CLIENT_VERSION" ]; then
            echo "❌ Could not determine Prisma versions"
            exit 1
          fi
          
          if echo "$PRISMA_VERSION" | grep -qE "^7\." || echo "$CLIENT_VERSION" | grep -qE "^7\."; then
            echo "❌ Wrong Prisma version! Expected 6.x, got 7.x"
            echo "   CLI: $PRISMA_VERSION"
            echo "   Client: $CLIENT_VERSION"
            exit 1
          fi
          
          if ! echo "$PRISMA_VERSION" | grep -qE "^6\." || ! echo "$CLIENT_VERSION" | grep -qE "^6\."; then
            echo "❌ Unexpected Prisma version! Expected 6.x"
            echo "   CLI: $PRISMA_VERSION"
            echo "   Client: $CLIENT_VERSION"
            exit 1
          fi
          
          echo "✅ Prisma 6.18.0 installed and verified"

      - name: Generate Prisma Client
        env:
          SKIP_DB_VALIDATION: true
          SKIP_POSTINSTALL: true
          DATABASE_URL: "postgresql://ci:ci@127.0.0.1:5432/ci"
        run: |
          echo "🔧 Generating Prisma Client..."
          [ -f prisma/schema.prisma ] || { echo "❌ schema.prisma not found"; exit 1; }
          
          # Verify Prisma version one more time before generation
          PRISMA_VERSION=$(./node_modules/.bin/prisma --version 2>/dev/null | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1 || echo "")
          echo "   Using Prisma version: $PRISMA_VERSION"
          
          if echo "$PRISMA_VERSION" | grep -qE "^7\."; then
            echo "❌ ERROR: Prisma 7.x detected! This version doesn't support url in schema.prisma"
            echo "   Please ensure Prisma 6.18.0 is installed"
            exit 1
          fi
          
          if ! echo "$PRISMA_VERSION" | grep -qE "^6\."; then
            echo "❌ ERROR: Unexpected Prisma version: $PRISMA_VERSION"
            echo "   Expected Prisma 6.x"
            exit 1
          fi
          
          # Clean any previous generation
          rm -rf node_modules/.prisma 2>/dev/null || true
          
          # Generate Prisma client using local binary
          echo "📝 Generating Prisma client with Prisma 6.18.0..."
          ./node_modules/.bin/prisma generate --schema=prisma/schema.prisma || {
            echo "⚠️  Local prisma failed, trying npx with explicit version..."
            npx prisma@6.18.0 generate --schema=prisma/schema.prisma || {
              echo "❌ Prisma generate failed"
              echo "   Error details above"
              exit 1
            }
          }
          
          # Verify client was generated
          [ -d node_modules/.prisma/client ] || [ -d node_modules/@prisma/client ] || \
            { echo "❌ Prisma client not generated"; exit 1; }
          
          echo "✅ Prisma client generated successfully with Prisma 6.18.0"

      - name: Build Next.js
        timeout-minutes: 12
        env:
          NODE_ENV: production
          NEXTAUTH_SECRET: ${{ env.NEXTAUTH_SECRET }}
          NEXTAUTH_URL: ${{ env.PROD_URL }}
          NEXT_PUBLIC_APP_URL: ${{ env.PROD_URL }}
          DATABASE_URL: "postgresql://ci:ci@127.0.0.1:5432/ci"
          NEXT_TELEMETRY_DISABLED: 1
          SKIP_ENV_VALIDATION: 1
          SKIP_DB_VALIDATION: true
          NODE_OPTIONS: --max-old-space-size=6144
          ESLINT_NO_DEV_ERRORS: true
          OPENAI_API_KEY: ${{ env.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ env.GEMINI_API_KEY }}
        run: |
          echo "🔨 Building Next.js..."
          echo "📋 Build environment:"
          echo "   NODE_ENV: $NODE_ENV"
          echo "   DATABASE_URL: ${DATABASE_URL:0:20}..."
          echo "   NEXTAUTH_URL: $NEXTAUTH_URL"
          
          # Build with full output to see all pages being built
          npm run build 2>&1 | tee build.log || {
            echo "⚠️  npm run build failed, trying npx next build..."
            npx next build 2>&1 | tee build.log || { 
              echo "❌ Build failed"
              echo "📋 Last 50 lines of build log:"
              tail -50 build.log || true
              exit 1
            }
          }
          
          [ -d .next ] || { echo "❌ .next not created"; exit 1; }
          FILE_COUNT=$(find .next -type f 2>/dev/null | wc -l)
          [ "$FILE_COUNT" -gt 50 ] || { echo "❌ .next appears empty ($FILE_COUNT files)"; exit 1; }
          
          echo "✅ Build successful ($FILE_COUNT files)"
          echo "📋 Checking build output for routes..."
          if [ -f .next/routes-manifest.json ]; then
            echo "   Routes manifest found"
            cat .next/routes-manifest.json | head -20 || true
          fi
          
          # Check if pages are in the build output
          echo "📋 Checking for built pages..."
          if [ -d .next/server/app ]; then
            echo "   Found app directory pages:"
            find .next/server/app -name "page.js" -o -name "page.js.map" 2>/dev/null | head -20 || true
            PAGE_COUNT=$(find .next/server/app -name "page.js" 2>/dev/null | wc -l)
            echo "   Total pages found: $PAGE_COUNT"
            if [ "$PAGE_COUNT" -lt 10 ]; then
              echo "⚠️  WARNING: Very few pages found in build output ($PAGE_COUNT)"
              echo "   This might indicate pages are not being built properly"
            fi
          fi
          
          # CRITICAL: Verify static files are generated
          echo "📋 Checking for static files..."
          if [ -d .next/static ]; then
            STATIC_COUNT=$(find .next/static -type f 2>/dev/null | wc -l)
            echo "   Static files found: $STATIC_COUNT"
            if [ "$STATIC_COUNT" -lt 10 ]; then
              echo "⚠️  WARNING: Very few static files found ($STATIC_COUNT)"
              echo "   This will cause CSS/JS 404 errors in production!"
            else
              echo "✅ Static files generated successfully"
              # Show sample static files
              echo "   Sample static files:"
              find .next/static -type f 2>/dev/null | head -10 || true
            fi
          else
            echo "❌ CRITICAL: .next/static directory not found!"
            echo "   This will cause all CSS/JS files to return 404 errors!"
            exit 1
          fi
          
          # Check for standalone build
          if [ -d .next/standalone ]; then
            echo "✅ Standalone build found"
            STANDALONE_COUNT=$(find .next/standalone -type f 2>/dev/null | wc -l)
            echo "   Standalone files: $STANDALONE_COUNT"
          else
            echo "⚠️  Standalone build not found (using standard Next.js mode)"
          fi

      - name: Create deployment bundle
        run: |
          echo "📦 Creating bundle..."
          BUNDLE_DIR="/tmp/deploy-$$"
          mkdir -p "$BUNDLE_DIR"
          
          # Copy all files to bundle directory first (static snapshot)
          echo "📦 Copying files to bundle directory..."
          
          # CRITICAL: Verify .next/static exists before copying
          if [ ! -d .next/static ]; then
            echo "❌ CRITICAL: .next/static directory not found!"
            echo "   Static files are required for CSS/JS to work in production"
            exit 1
          fi
          
          STATIC_FILE_COUNT=$(find .next/static -type f 2>/dev/null | wc -l)
          echo "   Found $STATIC_FILE_COUNT static files to copy"
          
          cp -r .next "$BUNDLE_DIR/" || { echo "❌ Failed to copy .next"; exit 1; }
          
          # Verify static files were copied
          if [ ! -d "$BUNDLE_DIR/.next/static" ]; then
            echo "❌ CRITICAL: Static files not copied to bundle!"
            exit 1
          fi
          
          BUNDLE_STATIC_COUNT=$(find "$BUNDLE_DIR/.next/static" -type f 2>/dev/null | wc -l)
          echo "   Copied $BUNDLE_STATIC_COUNT static files to bundle"
          
          if [ "$BUNDLE_STATIC_COUNT" -lt 10 ]; then
            echo "⚠️  WARNING: Very few static files in bundle ($BUNDLE_STATIC_COUNT)"
            echo "   This may cause CSS/JS 404 errors in production"
          fi
          
          # Delete source maps in a separate step and wait for completion
          echo "🧹 Removing source maps..."
          find "$BUNDLE_DIR/.next" -name "*.map" -type f -delete
          sync  # Force filesystem sync to ensure deletions are complete
          
          # Copy remaining files - CRITICAL: All must be present
          echo "📦 Copying required files to bundle..."
          
          # Verify and copy package.json
          if [ ! -f package.json ]; then
            echo "❌ package.json not found!"
            exit 1
          fi
          cp package.json "$BUNDLE_DIR/" || { echo "❌ Failed to copy package.json"; exit 1; }
          
          # Verify and copy package-lock.json
          if [ ! -f package-lock.json ]; then
            echo "⚠️  package-lock.json not found (will be regenerated)"
          else
            cp package-lock.json "$BUNDLE_DIR/" || { echo "❌ Failed to copy package-lock.json"; exit 1; }
          fi
          
          # CRITICAL: Verify and copy ecosystem.config.cjs
          if [ ! -f ecosystem.config.cjs ]; then
            echo "❌ CRITICAL: ecosystem.config.cjs not found!"
            echo "   This file is required for PM2 to start the application"
            exit 1
          fi
          cp ecosystem.config.cjs "$BUNDLE_DIR/" || { echo "❌ Failed to copy ecosystem.config.cjs"; exit 1; }
          echo "✅ ecosystem.config.cjs copied"
          
          # Verify and copy next.config.mjs
          if [ ! -f next.config.mjs ]; then
            echo "⚠️  next.config.mjs not found"
          else
            cp next.config.mjs "$BUNDLE_DIR/" || { echo "❌ Failed to copy next.config.mjs"; exit 1; }
          fi
          
          # Copy public directory if it exists
          if [ -d public ]; then
            cp -r public "$BUNDLE_DIR/" || { echo "❌ Failed to copy public directory"; exit 1; }
            echo "✅ public directory copied"
          else
            echo "⚠️  public directory not found"
          fi
          
          # Copy prisma directory if it exists
          if [ -d prisma ]; then
            cp -r prisma "$BUNDLE_DIR/" || { echo "❌ Failed to copy prisma directory"; exit 1; }
            echo "✅ prisma directory copied"
          else
            echo "⚠️  prisma directory not found"
          fi
          
          # Verify all critical files are in bundle
          echo "🔍 Verifying critical files in bundle..."
          if [ ! -f "$BUNDLE_DIR/ecosystem.config.cjs" ]; then
            echo "❌ CRITICAL: ecosystem.config.cjs not in bundle!"
            exit 1
          fi
          if [ ! -d "$BUNDLE_DIR/.next" ]; then
            echo "❌ CRITICAL: .next directory not in bundle!"
            exit 1
          fi
          if [ ! -d "$BUNDLE_DIR/.next/static" ]; then
            echo "❌ CRITICAL: .next/static directory not in bundle!"
            exit 1
          fi
          echo "✅ All critical files verified in bundle"
          
          # Force filesystem sync to ensure all copy operations are complete
          sync
          
          # Wait to ensure all file operations are fully committed to disk
          sleep 2
          
          # Verify bundle directory exists and has content
          if [ ! -d "$BUNDLE_DIR" ]; then
            echo "❌ Bundle directory does not exist: $BUNDLE_DIR"
            exit 1
          fi
          
          if [ -z "$(ls -A "$BUNDLE_DIR" 2>/dev/null)" ]; then
            echo "❌ Bundle directory is empty: $BUNDLE_DIR"
            exit 1
          fi
          
          echo "📦 Creating archive from static bundle directory..."
          echo "   Bundle directory: $BUNDLE_DIR"
          echo "   Bundle contents:"
          ls -la "$BUNDLE_DIR" | head -20 || true
          
          # Create tar archive INSIDE the bundle directory
          # CRITICAL: Specify full path for output file to create it in $BUNDLE_DIR
          # Use --warning=no-file-changed to ignore warnings about files changing
          # This is safe because we've already synced all file operations
          echo "📦 Creating tar archive..."
          tar -czf "$BUNDLE_DIR/release.tar.gz" -C "$BUNDLE_DIR" . --warning=no-file-changed 2>&1 || {
            # Check if tar actually failed or just warned
            if [ -f "$BUNDLE_DIR/release.tar.gz" ]; then
              echo "⚠️  tar reported warnings but archive was created successfully"
            else
              echo "❌ tar failed - archive was not created"
              echo "   Current directory: $(pwd)"
              echo "   Bundle directory: $BUNDLE_DIR"
              echo "   Bundle directory contents:"
              ls -la "$BUNDLE_DIR" || true
              exit 1
            fi
          }
          
          # Verify archive was created
          if [ ! -f "$BUNDLE_DIR/release.tar.gz" ]; then
            echo "❌ Archive was not created at $BUNDLE_DIR/release.tar.gz"
            echo "   Current directory: $(pwd)"
            echo "   Files in current directory:"
            ls -la . | grep -E "release|tar" || true
            echo "   Files in bundle directory:"
            ls -la "$BUNDLE_DIR" | grep -E "release|tar" || true
            exit 1
          fi
          
          # Move archive to current directory
          echo "📦 Moving archive to current directory..."
          mv "$BUNDLE_DIR/release.tar.gz" . || { 
            echo "❌ mv failed"
            echo "   Source: $BUNDLE_DIR/release.tar.gz"
            echo "   Source exists: $([ -f "$BUNDLE_DIR/release.tar.gz" ] && echo 'yes' || echo 'no')"
            echo "   Current directory: $(pwd)"
            exit 1
          }
          
          # Clean up bundle directory
          rm -rf "$BUNDLE_DIR"
          
          [ -f release.tar.gz ] || { echo "❌ release.tar.gz not created"; exit 1; }
          SIZE=$(du -h release.tar.gz | cut -f1)
          echo "✅ Bundle created: $SIZE"

      - name: Upload bundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: deployment-bundle
          path: release.tar.gz
          retention-days: 1

  deploy:
    name: "🚀 Deploy to Production (Zero-Downtime)"
    runs-on: ubuntu-latest
    needs: build
    outputs:
      bundle_name: ${{ steps.upload-bundle.outputs.bundle_name }}
    steps:
      - name: Download bundle
        uses: actions/download-artifact@v4
        with:
          name: deployment-bundle
          path: .
        
      - name: Verify bundle downloaded
        run: |
          if [ ! -f release.tar.gz ]; then
            echo "❌ release.tar.gz not found after download"
            echo "   Current directory: $(pwd)"
            echo "   Files in directory:"
            ls -la . || true
            exit 1
          fi
          SIZE=$(du -h release.tar.gz | cut -f1)
          echo "✅ Bundle downloaded: release.tar.gz ($SIZE)"

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          
          # Handle both escaped and unescaped newlines
          echo "$SSH_KEY" | sed 's/\\n/\n/g' > ~/.ssh/deploy_key
          sed -i 's/\r$//' ~/.ssh/deploy_key  # Remove CRLF
          chmod 600 ~/.ssh/deploy_key
          
          # Validate SSH key
          grep -q "BEGIN.*PRIVATE" ~/.ssh/deploy_key || { echo "❌ Invalid SSH key"; exit 1; }
          grep -q "END.*PRIVATE" ~/.ssh/deploy_key || { echo "❌ SSH key missing END marker"; exit 1; }
          echo "✅ SSH key valid ($(wc -c < ~/.ssh/deploy_key) bytes)"

      - name: Test SSH connection
        run: |
          SSH_OPTS="-i ~/.ssh/deploy_key -p $SSH_PORT -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15 -o IdentitiesOnly=yes -o ServerAliveInterval=60 -o ServerAliveCountMax=3"
          ssh $SSH_OPTS "$SSH_USER@$HOST" "echo '✅ SSH OK' && uname -a" || {
            echo "❌ SSH failed - check SSH_KEY, HOST, SSH_PORT, SSH_USER"
            exit 1
          }

      - name: Upload bundle to staging
        run: |
          set -e  # Exit on any error
          SSH_OPTS="-i ~/.ssh/deploy_key -p $SSH_PORT -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15 -o IdentitiesOnly=yes -o BatchMode=yes"
          
          # Verify local bundle exists before uploading
          if [ ! -f release.tar.gz ]; then
            echo "❌ release.tar.gz not found in current directory"
            echo "   Current directory: $(pwd)"
            ls -la . | grep -E "release|tar" || echo "   (no release files found)"
            exit 1
          fi
          
          LOCAL_SIZE=$(du -h release.tar.gz | cut -f1)
          echo "📦 Local bundle verified: release.tar.gz ($LOCAL_SIZE)"
          
          # Create folders and verify they exist
          echo "📁 Creating remote folders..."
          ssh $SSH_OPTS "$SSH_USER@$HOST" "mkdir -p $DEPLOY_FOLDER $STAGING_FOLDER $BACKUP_FOLDER" || {
            echo "❌ Failed to create remote folders"
            exit 1
          }
          
          # Verify folders were created
          ssh $SSH_OPTS "$SSH_USER@$HOST" "test -d $STAGING_FOLDER" || {
            echo "❌ Staging folder does not exist after creation: $STAGING_FOLDER"
            exit 1
          }
          echo "✅ Remote folders verified"
          
          # Generate bundle name
          TIMESTAMP=$(date +%s)
          BUNDLE_NAME="release-$TIMESTAMP.tar.gz"
          STAGING_PATH="$STAGING_FOLDER/$BUNDLE_NAME"
          
          echo "📦 Uploading bundle: $BUNDLE_NAME"
          echo "   Destination: $STAGING_PATH"
          
          # Upload with verbose output
          rsync -avz --progress -e "ssh $SSH_OPTS" release.tar.gz "$SSH_USER@$HOST:$STAGING_PATH" || {
            echo "❌ rsync failed"
            echo "   Source: release.tar.gz"
            echo "   Destination: $SSH_USER@$HOST:$STAGING_PATH"
            exit 1
          }
          
          # Comprehensive verification - check file exists
          echo "🔍 Verifying upload (check 1/3): File existence..."
          ssh $SSH_OPTS "$SSH_USER@$HOST" "test -f $STAGING_PATH" || {
            echo "❌ Upload verification failed - file not found on remote server"
            echo "   Expected path: $STAGING_PATH"
            echo "   Staging folder contents:"
            ssh $SSH_OPTS "$SSH_USER@$HOST" "ls -la $STAGING_FOLDER 2>/dev/null || echo '   (staging folder does not exist or is not accessible)'" || true
            echo "   Staging folder permissions:"
            ssh $SSH_OPTS "$SSH_USER@$HOST" "ls -ld $STAGING_FOLDER 2>/dev/null || echo '   (cannot check permissions)'" || true
            exit 1
          }
          
          # Verify file size matches
          echo "🔍 Verifying upload (check 2/3): File size..."
          LOCAL_SIZE_BYTES=$(stat -f%z release.tar.gz 2>/dev/null || stat -c%s release.tar.gz 2>/dev/null || echo "0")
          REMOTE_SIZE_BYTES=$(ssh $SSH_OPTS "$SSH_USER@$HOST" "stat -f%z $STAGING_PATH 2>/dev/null || stat -c%s $STAGING_PATH 2>/dev/null || echo '0'")
          
          if [ "$LOCAL_SIZE_BYTES" != "$REMOTE_SIZE_BYTES" ]; then
            echo "❌ File size mismatch!"
            echo "   Local: $LOCAL_SIZE_BYTES bytes"
            echo "   Remote: $REMOTE_SIZE_BYTES bytes"
            exit 1
          fi
          
          # Verify file is readable
          echo "🔍 Verifying upload (check 3/3): File readability..."
          ssh $SSH_OPTS "$SSH_USER@$HOST" "test -r $STAGING_PATH" || {
            echo "❌ File is not readable on remote server"
            exit 1
          }
          
          REMOTE_SIZE=$(ssh $SSH_OPTS "$SSH_USER@$HOST" "du -h $STAGING_PATH | cut -f1")
          echo "✅ Bundle uploaded and fully verified: $STAGING_PATH ($REMOTE_SIZE, $REMOTE_SIZE_BYTES bytes)"
          
          # Save bundle name to remote server with verification
          echo "💾 Saving bundle name to remote server..."
          ssh $SSH_OPTS "$SSH_USER@$HOST" "echo '$BUNDLE_NAME' > /tmp/bundle_name.txt" || {
            echo "❌ Failed to save bundle name to remote server"
            exit 1
          }
          
          # Verify remote bundle name was saved
          REMOTE_BUNDLE_NAME=$(ssh $SSH_OPTS "$SSH_USER@$HOST" "cat /tmp/bundle_name.txt 2>/dev/null | tr -d '\n\r ' || echo ''")
          if [ "$REMOTE_BUNDLE_NAME" != "$BUNDLE_NAME" ]; then
            echo "❌ Remote bundle name mismatch!"
            echo "   Expected: $BUNDLE_NAME"
            echo "   Got: $REMOTE_BUNDLE_NAME"
            exit 1
          fi
          echo "✅ Bundle name saved and verified on remote server: $BUNDLE_NAME"
          
          # Save bundle name locally for finalize step
          echo "$BUNDLE_NAME" > bundle_name.txt
          
          # Verify local bundle name was saved
          if [ ! -f bundle_name.txt ] || [ -z "$(cat bundle_name.txt | tr -d '\n\r ')" ]; then
            echo "❌ Failed to save bundle name locally"
            exit 1
          fi
          
          LOCAL_BUNDLE_NAME=$(cat bundle_name.txt | tr -d '\n\r ')
          if [ "$LOCAL_BUNDLE_NAME" != "$BUNDLE_NAME" ]; then
            echo "❌ Local bundle name mismatch!"
            echo "   Expected: $BUNDLE_NAME"
            echo "   Got: $LOCAL_BUNDLE_NAME"
            exit 1
          fi
          
          echo "✅ Bundle name saved and verified locally: $BUNDLE_NAME"

      - name: Save bundle name
        id: upload-bundle
        run: |
          set -e
          
          # Read bundle name from file
          if [ ! -f bundle_name.txt ]; then
            echo "❌ bundle_name.txt not found"
            exit 1
          fi
          
          BUNDLE_NAME=$(cat bundle_name.txt | tr -d '\n\r ' || echo "")
          if [ -z "$BUNDLE_NAME" ]; then
            echo "❌ BUNDLE_NAME is empty in bundle_name.txt"
            echo "   File contents: '$(cat bundle_name.txt)'"
            exit 1
          fi
          
          # Verify bundle still exists on remote server
          echo "🔍 Verifying bundle still exists on remote server..."
          SSH_OPTS="-i ~/.ssh/deploy_key -p $SSH_PORT -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15 -o IdentitiesOnly=yes -o BatchMode=yes"
          ssh $SSH_OPTS "$SSH_USER@$HOST" "test -f $STAGING_FOLDER/$BUNDLE_NAME" || {
            echo "❌ Bundle no longer exists on remote server!"
            echo "   Expected: $STAGING_FOLDER/$BUNDLE_NAME"
            echo "   This should not happen - bundle was just uploaded"
            exit 1
          }
          
          # Save to GitHub output for next job
          echo "bundle_name=$BUNDLE_NAME" >> $GITHUB_OUTPUT
          echo "✅ Bundle name set as job output: $BUNDLE_NAME"
          echo "✅ Bundle verified on remote server: $STAGING_FOLDER/$BUNDLE_NAME"
        
      - name: Upload bundle info artifact
        uses: actions/upload-artifact@v4
        with:
          name: bundle-info
          path: bundle_name.txt
          retention-days: 1
          
      - name: Final verification before job completion
        run: |
          set -e
          SSH_OPTS="-i ~/.ssh/deploy_key -p $SSH_PORT -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15 -o IdentitiesOnly=yes -o BatchMode=yes"
          
          BUNDLE_NAME=$(cat bundle_name.txt | tr -d '\n\r ')
          echo "🔍 Final verification: Bundle and metadata..."
          
          # Verify bundle file
          ssh $SSH_OPTS "$SSH_USER@$HOST" "test -f $STAGING_FOLDER/$BUNDLE_NAME" || {
            echo "❌ Final check failed: Bundle not found on remote server"
            exit 1
          }
          
          # Verify remote bundle name file
          REMOTE_BUNDLE_NAME=$(ssh $SSH_OPTS "$SSH_USER@$HOST" "cat /tmp/bundle_name.txt 2>/dev/null | tr -d '\n\r ' || echo ''")
          if [ "$REMOTE_BUNDLE_NAME" != "$BUNDLE_NAME" ]; then
            echo "❌ Final check failed: Remote bundle name mismatch"
            echo "   Expected: $BUNDLE_NAME"
            echo "   Got: $REMOTE_BUNDLE_NAME"
            exit 1
          fi
          
          echo "✅ All verifications passed - bundle ready for deployment"

  finalize:
    name: "✅ Finalize Deployment (Zero-Downtime Swap)"
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - name: Download bundle info
        uses: actions/download-artifact@v4
        with:
          name: bundle-info
          path: .
        
      - name: Verify bundle info downloaded
        run: |
          if [ ! -f bundle_name.txt ]; then
            echo "❌ bundle_name.txt not found after download"
            echo "   Current directory: $(pwd)"
            echo "   Files in directory:"
            ls -la . || true
            echo "   Trying to get bundle name from job output..."
            # Try to get from job output as fallback
            if [ -n "${{ needs.deploy.outputs.bundle_name }}" ]; then
              echo "${{ needs.deploy.outputs.bundle_name }}" > bundle_name.txt
              echo "✅ Used bundle name from job output: ${{ needs.deploy.outputs.bundle_name }}"
            else
              echo "❌ Could not determine bundle name"
              exit 1
            fi
          fi
          
          BUNDLE_NAME=$(cat bundle_name.txt | tr -d '\n\r ' || echo "")
          if [ -z "$BUNDLE_NAME" ]; then
            echo "❌ BUNDLE_NAME is empty in bundle_name.txt"
            echo "   File contents: '$(cat bundle_name.txt)'"
            exit 1
          fi
          
          echo "✅ Bundle name loaded: $BUNDLE_NAME"
          echo "BUNDLE_NAME=$BUNDLE_NAME" >> $GITHUB_ENV

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "$SSH_KEY" | sed 's/\\n/\n/g' > ~/.ssh/deploy_key
          sed -i 's/\r$//' ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

      - name: Deploy with zero-downtime swap
        env:
          BUNDLE_NAME: ${{ env.BUNDLE_NAME || 'unknown' }}
          STAGING_FOLDER: ${{ env.STAGING_FOLDER }}
          DEPLOY_FOLDER: ${{ env.DEPLOY_FOLDER }}
          BACKUP_FOLDER: ${{ env.BACKUP_FOLDER }}
        run: |
          set -e
          
          # Verify required environment variables are set
          if [ -z "$STAGING_FOLDER" ]; then
            echo "❌ STAGING_FOLDER is not set!"
            exit 1
          fi
          
          if [ -z "$DEPLOY_FOLDER" ]; then
            echo "❌ DEPLOY_FOLDER is not set!"
            exit 1
          fi
          
          if [ -z "$BACKUP_FOLDER" ]; then
            echo "❌ BACKUP_FOLDER is not set!"
            exit 1
          fi
          
          echo "📋 Environment variables:"
          echo "   STAGING_FOLDER: $STAGING_FOLDER"
          echo "   DEPLOY_FOLDER: $DEPLOY_FOLDER"
          echo "   BACKUP_FOLDER: $BACKUP_FOLDER"
          
          # Get bundle name from environment or file
          if [ -z "$BUNDLE_NAME" ] || [ "$BUNDLE_NAME" = "unknown" ]; then
            if [ -f bundle_name.txt ]; then
              BUNDLE_NAME=$(cat bundle_name.txt | tr -d '\n\r ' || echo "")
            fi
          fi
          
          # Final fallback - try to get from job output
          if [ -z "$BUNDLE_NAME" ]; then
            BUNDLE_NAME="${{ needs.deploy.outputs.bundle_name }}"
          fi
          
                 SSH_OPTS="-i ~/.ssh/deploy_key -p $SSH_PORT -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15 -o IdentitiesOnly=yes -o ServerAliveInterval=60 -o ServerAliveCountMax=3"
          
          # Verify bundle name is not empty
          if [ -z "$BUNDLE_NAME" ]; then
            echo "❌ BUNDLE_NAME is empty after all attempts!"
            echo "   Tried: environment variable, bundle_name.txt, job output"
            exit 1
          fi
          
          echo "🚀 Deploying $BUNDLE_NAME with zero-downtime swap..."
          echo "⏱️  Deployment timeout: 15 minutes (900 seconds)"
          
          # Remote deployment script - pass all required environment variables
          # Export variables in the SSH command and use single-quoted heredoc to prevent local expansion
          # CRITICAL: Add timeout to prevent hanging indefinitely
          timeout 900 ssh $SSH_OPTS "$SSH_USER@$HOST" \
            "BUNDLE_NAME='$BUNDLE_NAME' \
             STAGING_FOLDER='$STAGING_FOLDER' \
             DEPLOY_FOLDER='$DEPLOY_FOLDER' \
             BACKUP_FOLDER='$BACKUP_FOLDER' \
             DATABASE_URL='$DATABASE_URL' \
             bash -s" << 'REMOTE_SCRIPT'
          set -e
          
          # Verify all required environment variables are set
          if [ -z "$STAGING_FOLDER" ]; then
            echo "❌ STAGING_FOLDER is not set in remote script!"
            exit 1
          fi
          
          if [ -z "$DEPLOY_FOLDER" ]; then
            echo "❌ DEPLOY_FOLDER is not set in remote script!"
            exit 1
          fi
          
          if [ -z "$BACKUP_FOLDER" ]; then
            echo "❌ BACKUP_FOLDER is not set in remote script!"
            exit 1
          fi
          
          echo "📋 Remote environment variables:"
          echo "   STAGING_FOLDER: $STAGING_FOLDER"
          echo "   DEPLOY_FOLDER: $DEPLOY_FOLDER"
          echo "   BACKUP_FOLDER: $BACKUP_FOLDER"
          echo "   BUNDLE_NAME: $BUNDLE_NAME"
          # Environment variables are already set and verified above
          
          # Use BUNDLE_NAME from environment, or try to find it
          if [ -z "$BUNDLE_NAME" ]; then
            echo "⚠️  BUNDLE_NAME not set, trying to read from remote server..."
            # Try to read from remote server first (most reliable)
            BUNDLE_NAME=$(cat /tmp/bundle_name.txt 2>/dev/null | tr -d '\n\r ' || echo "")
          fi
          
          if [ -z "$BUNDLE_NAME" ]; then
            echo "⚠️  Not found in /tmp/bundle_name.txt, trying to find latest bundle..."
            BUNDLE_NAME=$(ls -t $STAGING_FOLDER/*.tar.gz 2>/dev/null | head -1 | xargs basename || echo "")
          fi
          
          if [ -z "$BUNDLE_NAME" ]; then
            echo "❌ Could not determine BUNDLE_NAME"
            echo "   Tried: environment variable, /tmp/bundle_name.txt, latest file in staging folder"
            echo "   Staging folder: $STAGING_FOLDER"
            echo "   Available files:"
            ls -la "$STAGING_FOLDER" 2>/dev/null || echo "   (staging folder does not exist or is empty)"
            echo "   /tmp/bundle_name.txt contents:"
            cat /tmp/bundle_name.txt 2>/dev/null || echo "   (file does not exist)"
            exit 1
          fi
          
          STAGING_PATH="$STAGING_FOLDER/$BUNDLE_NAME"
          TEMP_DEPLOY="/var/www/naukrimili-temp-$$"
          PROD_DEPLOY="$DEPLOY_FOLDER"
          BACKUP_PATH="$BACKUP_FOLDER/backup-$(date +%s)"
          
          echo "📦 Bundle: $BUNDLE_NAME"
          echo "📂 Staging path: $STAGING_PATH"
          echo "📂 Temp folder: $TEMP_DEPLOY"
          
          # Comprehensive bundle file verification
          echo "🔍 Verifying bundle file before extraction..."
          
          # Verify STAGING_FOLDER is set and not empty
          if [ -z "$STAGING_FOLDER" ]; then
            echo "❌ STAGING_FOLDER is empty or not set!"
            echo "   This should not happen - variable should be passed from GitHub Actions"
            exit 1
          fi
          
          echo "📂 Using staging folder: $STAGING_FOLDER"
          
          # Check if staging folder exists
          if [ ! -d "$STAGING_FOLDER" ]; then
            echo "⚠️  Staging folder does not exist: $STAGING_FOLDER"
            echo "   Attempting to create it..."
            mkdir -p "$STAGING_FOLDER" || {
              echo "❌ Failed to create staging folder: $STAGING_FOLDER"
              echo "   Parent directory: $(dirname "$STAGING_FOLDER")"
              echo "   Parent exists: $([ -d "$(dirname "$STAGING_FOLDER")" ] && echo 'yes' || echo 'no')"
              exit 1
            }
            echo "✅ Staging folder created: $STAGING_FOLDER"
          else
            echo "✅ Staging folder exists: $STAGING_FOLDER"
          fi
          
          # Verify bundle file exists
          if [ ! -f "$STAGING_PATH" ]; then
            echo "❌ Bundle file not found at $STAGING_PATH"
            echo "   Staging folder: $STAGING_FOLDER"
            echo "   Staging folder exists: $([ -d "$STAGING_FOLDER" ] && echo 'yes' || echo 'no')"
            echo "   Staging folder permissions:"
            ls -ld "$STAGING_FOLDER" 2>/dev/null || echo "   (cannot check permissions)"
            echo "   Available files in staging folder:"
            ls -la "$STAGING_FOLDER" 2>/dev/null || echo "   (staging folder is empty or not accessible)"
            echo "   Looking for any .tar.gz files:"
            find "$STAGING_FOLDER" -name "*.tar.gz" -type f 2>/dev/null | head -5 || echo "   (no .tar.gz files found)"
            exit 1
          fi
          
          # Verify file is not empty
          FILE_SIZE=$(stat -f%z "$STAGING_PATH" 2>/dev/null || stat -c%s "$STAGING_PATH" 2>/dev/null || echo "0")
          if [ "$FILE_SIZE" -eq 0 ]; then
            echo "❌ Bundle file is empty: $STAGING_PATH"
            exit 1
          fi
          
          # Verify file is readable
          if [ ! -r "$STAGING_PATH" ]; then
            echo "❌ Bundle file is not readable: $STAGING_PATH"
            exit 1
          fi
          
          FILE_SIZE_HUMAN=$(du -h "$STAGING_PATH" | cut -f1)
          echo "✅ Bundle file verified: $STAGING_PATH ($FILE_SIZE_HUMAN, $FILE_SIZE bytes)"
          
          # Extract to temp folder
          mkdir -p "$TEMP_DEPLOY"
          cd "$TEMP_DEPLOY"
          echo "📦 Extracting bundle..."
          tar -xzf "$STAGING_PATH" || { 
            echo "❌ Extract failed"
            echo "   Archive path: $STAGING_PATH"
            echo "   Archive exists: $([ -f "$STAGING_PATH" ] && echo 'yes' || echo 'no')"
            echo "   Archive size: $(du -h "$STAGING_PATH" | cut -f1)"
            exit 1
          }
          echo "✅ Bundle extracted"
          
          # CRITICAL: Verify critical files and directories after extraction
          echo "🔍 Verifying extracted bundle contents..."
          
          # Verify .next directory exists
          if [ ! -d ".next" ]; then
            echo "❌ CRITICAL: .next directory not found after extraction!"
            echo "   Current directory: $(pwd)"
            echo "   Contents of extraction directory:"
            ls -la . | head -20 || true
            exit 1
          fi
          echo "✅ .next directory found"
          
          # Verify .next/static directory exists
          if [ ! -d ".next/static" ]; then
            echo "❌ CRITICAL: .next/static directory not found after extraction!"
            echo "   This will cause all CSS/JS files to return 404 errors!"
            echo "   .next directory contents:"
            ls -la .next 2>/dev/null || echo "   (cannot list .next directory)"
            exit 1
          fi
          echo "✅ .next/static directory found"
          
          # Verify static files count
          STATIC_COUNT=$(find .next/static -type f 2>/dev/null | wc -l)
          echo "   Static files found: $STATIC_COUNT"
          if [ "$STATIC_COUNT" -lt 10 ]; then
            echo "❌ CRITICAL: Very few static files found ($STATIC_COUNT)"
            echo "   This will cause CSS/JS 404 errors!"
            echo "   Static directory contents:"
            ls -la .next/static 2>/dev/null || echo "   (cannot list static directory)"
            exit 1
          else
            echo "✅ Static files verified: $STATIC_COUNT files"
            # Show sample CSS and JS files
            CSS_COUNT=$(find .next/static -name "*.css" 2>/dev/null | wc -l)
            JS_COUNT=$(find .next/static -name "*.js" 2>/dev/null | wc -l)
            echo "   CSS files: $CSS_COUNT"
            echo "   JS files: $JS_COUNT"
          fi
          
          # Verify ecosystem.config.cjs exists
          if [ ! -f "ecosystem.config.cjs" ]; then
            echo "❌ CRITICAL: ecosystem.config.cjs not found after extraction!"
            echo "   This file is required for PM2 to start the application"
            echo "   Current directory: $(pwd)"
            echo "   Contents of extraction directory:"
            ls -la . | head -20 || true
            exit 1
          fi
          echo "✅ ecosystem.config.cjs found"
          
          # Verify package.json exists (needed for npm install)
          if [ ! -f "package.json" ]; then
            echo "❌ CRITICAL: package.json not found after extraction!"
            echo "   This file is required for installing dependencies"
            exit 1
          fi
          echo "✅ package.json found"
          
          # Install dependencies (skip postinstall to avoid prisma generate during install)
          if [ -f package-lock.json ]; then
            echo "📚 Installing dependencies..."
            export SKIP_POSTINSTALL=true
            npm ci --omit=dev --prefer-offline --legacy-peer-deps --ignore-scripts 2>&1 | tail -10 || {
              echo "⚠️  npm ci had issues, trying npm install..."
              npm install --omit=dev --legacy-peer-deps --prefer-offline --ignore-scripts 2>&1 | tail -10 || {
                echo "❌ npm install failed"
                exit 1
              }
            }
            
            # Verify critical dependencies are installed
            if [ ! -d "node_modules" ]; then
              echo "❌ node_modules directory not created"
              exit 1
            fi
            
            # Verify key packages
            if [ ! -d "node_modules/next" ]; then
              echo "❌ Next.js not installed"
              exit 1
            fi
            
            echo "✅ Dependencies installed and verified"
            
            # Generate Prisma client separately (after dependencies are installed)
            if [ -f prisma/schema.prisma ] && [ -d "node_modules/prisma" ]; then
              echo "🔧 Generating Prisma Client..."
              # DATABASE_URL should be available from environment, use a safe default if not
              export DATABASE_URL="${DATABASE_URL:-postgresql://localhost:5432/naukrimili}"
              npx prisma generate --schema=prisma/schema.prisma 2>&1 | tail -10 || {
                echo "⚠️  Prisma generate had issues, but continuing..."
              }
            fi
          else
            echo "⚠️  package-lock.json not found, skipping dependency installation"
          fi
          
          # Run migrations if schema changed
          if [ -f prisma/schema.prisma ]; then
            export NODE_ENV=production
            # DATABASE_URL should already be set from environment
            echo "🗄️  Running migrations..."
            timeout 60 npx prisma migrate deploy 2>&1 | tail -5 || echo "⚠️  Migration warning"
          fi
          
          # Start PM2 in temp folder to test
          echo "🧪 Testing application in temp folder..."
          
          # Clean up PM2 state - delete all processes and reset PM2
          echo "🧹 Cleaning up PM2 state..."
          pm2 kill 2>/dev/null || true
          pm2 flush 2>/dev/null || true
          
          # Delete any existing test process (try multiple names)
          pm2 delete naukrimili-test 2>/dev/null || true
          pm2 delete jobportal-test 2>/dev/null || true
          pm2 delete naukrimili 2>/dev/null || true
          pm2 delete jobportal 2>/dev/null || true
          pm2 delete all 2>/dev/null || true
          
          # Wait a moment for PM2 to clean up
          sleep 2
          
          # Verify ecosystem.config.cjs exists
          if [ ! -f ecosystem.config.cjs ]; then
            echo "❌ ecosystem.config.cjs not found in $TEMP_DEPLOY"
            ls -la "$TEMP_DEPLOY" | head -20 || true
            exit 1
          fi
          
          # Verify .next directory exists
          if [ ! -d ".next" ]; then
            echo "❌ .next directory not found in $TEMP_DEPLOY"
            ls -la "$TEMP_DEPLOY" | head -20 || true
            exit 1
          fi
          
          # Start with test name override
          echo "🚀 Starting PM2 with test name..."
          if pm2 start ecosystem.config.cjs --name naukrimili-test --env production --update-env; then
            echo "⏳ Waiting for PM2 process to be ready..."
            sleep 5
            
            # Verify PM2 process is running
            PM2_READY=false
            for i in {1..30}; do
              if pm2 status | grep -q "naukrimili-test.*online"; then
                PM2_READY=true
                echo "✅ PM2 process is online (attempt $i/30)"
                break
              fi
              echo "   Waiting for PM2 process... (attempt $i/30)"
              sleep 2
            done
            
            if [ "$PM2_READY" != "true" ]; then
              echo "❌ PM2 process did not start properly"
              pm2 status || true
              pm2 logs naukrimili-test --lines 50 || true
              exit 1
            fi
            
            # Health check with timeout and retries
            echo "🏥 Running health check..."
            HEALTH_CHECK_PASSED=false
            for i in {1..20}; do
              if timeout 5 curl -f -s http://localhost:3000/api/health > /dev/null 2>&1; then
                HEALTH_CHECK_PASSED=true
                echo "✅ Health check passed (attempt $i/20)"
                break
              fi
              echo "   Health check failed, retrying... (attempt $i/20)"
              sleep 3
            done
            
            if [ "$HEALTH_CHECK_PASSED" != "true" ]; then
              echo "❌ Health check failed after 20 attempts"
              echo "📋 PM2 Status:"
              pm2 status || true
              echo "📋 PM2 Logs:"
              pm2 logs naukrimili-test --lines 50 || true
              echo "📋 Checking if port 3000 is listening:"
              netstat -tlnp 2>/dev/null | grep 3000 || ss -tlnp 2>/dev/null | grep 3000 || echo "   Port 3000 not listening"
              exit 1
            fi
            
            if [ "$HEALTH_CHECK_PASSED" = "true" ]; then
              echo "✅ Health check passed"
              
              # Success: backup current and swap
              echo "🔄 Swapping to production..."
              pm2 delete naukrimili-test 2>/dev/null || true
              pm2 delete jobportal-test 2>/dev/null || true
              
              if [ -d "$PROD_DEPLOY/.next" ]; then
                echo "💾 Backing up current version..."
                mkdir -p "$BACKUP_PATH"
                cp -r "$PROD_DEPLOY/.next" "$BACKUP_PATH/" || true
                cp -r "$PROD_DEPLOY/node_modules" "$BACKUP_PATH/" 2>/dev/null || true
              fi
              
              # Atomic swap
              rm -rf "$PROD_DEPLOY.old"
              [ -d "$PROD_DEPLOY" ] && mv "$PROD_DEPLOY" "$PROD_DEPLOY.old" || true
              mv "$TEMP_DEPLOY" "$PROD_DEPLOY"
              rm -rf "$PROD_DEPLOY.old" "$TEMP_DEPLOY"
              
              # Start production PM2
              cd "$PROD_DEPLOY"
              
              # Verify ecosystem.config.cjs exists
              if [ ! -f ecosystem.config.cjs ]; then
                echo "❌ ecosystem.config.cjs not found in $PROD_DEPLOY"
                exit 1
              fi
              
              # Clean up PM2 state before starting production
              echo "🧹 Cleaning up PM2 before production start..."
              pm2 delete naukrimili 2>/dev/null || true
              pm2 delete jobportal 2>/dev/null || true
              pm2 delete all 2>/dev/null || true
              sleep 1
              
              # Start PM2 with ecosystem config (uses name from config: "naukrimili")
              echo "🚀 Starting PM2 with ecosystem.config.cjs..."
              pm2 start ecosystem.config.cjs --env production --update-env || {
                echo "❌ PM2 start failed"
                echo "   Current directory: $(pwd)"
                echo "   Files in directory:"
                ls -la . | head -10 || true
                echo "   PM2 status:"
                pm2 status || true
                echo "   Attempting rollback..."
                [ -d "$BACKUP_PATH/.next" ] && {
                  echo "   Restoring backup..."
                  cp -r "$BACKUP_PATH/.next" "$PROD_DEPLOY/"
                  pm2 start ecosystem.config.cjs --env production || {
                    echo "❌ Rollback PM2 start also failed"
                    exit 1
                  }
                } || {
                  echo "❌ No backup available for rollback"
                  exit 1
                }
              }
              
              # Verify PM2 process started with retries
              echo "⏳ Waiting for production PM2 process to be ready..."
              sleep 5
              
              PM2_READY=false
              for i in {1..30}; do
                if pm2 status | grep -q "naukrimili.*online"; then
                  PM2_READY=true
                  echo "✅ Production PM2 process is online (attempt $i/30)"
                  break
                fi
                echo "   Waiting for production PM2 process... (attempt $i/30)"
                sleep 2
              done
              
              if [ "$PM2_READY" != "true" ]; then
                echo "❌ Production PM2 process did not start properly"
                echo "   PM2 status:"
                pm2 status || true
                echo "   PM2 logs:"
                pm2 logs naukrimili --lines 50 || true
                exit 1
              fi
              
              pm2 save --force
              sleep 2
              
              # Production health check with timeout and retries
              echo "🏥 Running production health check..."
              HEALTH_CHECK_PASSED=false
              for i in {1..20}; do
                if timeout 5 curl -f -s http://localhost:3000/api/health > /dev/null 2>&1; then
                  HEALTH_CHECK_PASSED=true
                  echo "✅ Production health check passed (attempt $i/20)"
                  break
                fi
                echo "   Production health check failed, retrying... (attempt $i/20)"
                sleep 3
              done
              
              if [ "$HEALTH_CHECK_PASSED" != "true" ]; then
                echo "❌ Production health check failed after 20 attempts"
                echo "📋 PM2 Status:"
                pm2 status || true
                echo "📋 PM2 Logs:"
                pm2 logs naukrimili --lines 50 || true
                echo "📋 Checking if port 3000 is listening:"
                netstat -tlnp 2>/dev/null | grep 3000 || ss -tlnp 2>/dev/null | grep 3000 || echo "   Port 3000 not listening"
                exit 1
              fi
              
              if [ "$HEALTH_CHECK_PASSED" = "true" ]; then
                echo "✅ Production deployment successful"
                # Clean old backups (keep last 3)
                cd "$BACKUP_FOLDER"
                ls -t | tail -n +4 | xargs -r rm -rf
                rm -f "$STAGING_PATH"
              fi
            else
              echo "❌ Health check failed in temp folder"
              echo "   PM2 status:"
              pm2 status || true
              echo "   PM2 logs:"
              pm2 logs naukrimili-test --lines 20 || true
              pm2 logs jobportal-test --lines 20 || true
              rm -rf "$TEMP_DEPLOY"
              exit 1
            fi
          else
            echo "❌ Failed to start PM2 in temp folder"
            echo "   PM2 status:"
            pm2 status || true
            echo "   PM2 logs:"
            pm2 logs naukrimili-test --lines 50 || true
            pm2 logs jobportal-test --lines 50 || true
            echo "   Application files in $TEMP_DEPLOY:"
            ls -la "$TEMP_DEPLOY" | head -20 || true
            rm -rf "$TEMP_DEPLOY"
            exit 1
          fi
          REMOTE_SCRIPT
          
          echo "✅ Deployment complete - zero downtime achieved!"

      - name: Post-deployment verification
        run: |
          SSH_OPTS="-i ~/.ssh/deploy_key -p $SSH_PORT -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15"
          
          echo "🧪 Running post-deployment checks..."
          
          # Check PM2 status
          echo ""
          echo "📊 PM2 Status:"
          ssh $SSH_OPTS "$SSH_USER@$HOST" "pm2 status" || echo "⚠️  Could not get PM2 status"
          
          # Check health endpoint
          echo ""
          echo "🏥 Health Check:"
          ssh $SSH_OPTS "$SSH_USER@$HOST" "curl -s http://localhost:3000/api/health | head -c 200 || echo '⚠️  No response'" || true
          
          # Check disk usage
          echo ""
          echo "💾 Disk Usage:"
          ssh $SSH_OPTS "$SSH_USER@$HOST" "du -sh $DEPLOY_FOLDER $BACKUP_FOLDER 2>/dev/null || echo 'N/A'" || true
          
          echo ""
          echo "✅ Post-deployment verification complete"
