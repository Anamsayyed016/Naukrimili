name: Deploy to Production (Zero-Downtime)

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  HOST: ${{ secrets.HOST }}
  SSH_USER: ${{ secrets.SSH_USER }}
  SSH_KEY: ${{ secrets.SSH_KEY }}
  SSH_PORT: ${{ secrets.SSH_PORT }}
  NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
  DATABASE_URL: ${{ secrets.DATABASE_URL }}
  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
  GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
  GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
  GOOGLE_CLOUD_OCR_API_KEY: ${{ secrets.GOOGLE_CLOUD_OCR_API_KEY }}
  PROD_URL: https://naukrimili.com
  DEPLOY_FOLDER: /var/www/naukrimili
  STAGING_FOLDER: /var/www/naukrimili-staging
  BACKUP_FOLDER: /var/www/naukrimili-backup

jobs:
  validate_secrets:
    name: "🔐 Validate Secrets"
    runs-on: ubuntu-latest
    steps:
      - name: Validate All Required Secrets
        run: |
          set +e
          echo "════════════════════════════════════════════════════════════"
          echo "🔐 VALIDATING ALL PRODUCTION SECRETS"
          echo "════════════════════════════════════════════════════════════"
          echo ""
          
          MISSING=0
          
          # Check critical deployment secrets
          [ -z "$HOST" ] && echo "❌ HOST: MISSING" && MISSING=1 || echo "✅ HOST: SET (${#HOST} chars)"
          [ -z "$SSH_USER" ] && echo "❌ SSH_USER: MISSING" && MISSING=1 || echo "✅ SSH_USER: SET"
          [ -z "$SSH_PORT" ] && echo "❌ SSH_PORT: MISSING" && MISSING=1 || echo "✅ SSH_PORT: SET"
          [ -z "$SSH_KEY" ] && echo "❌ SSH_KEY: MISSING" && MISSING=1 || echo "✅ SSH_KEY: SET (${#SSH_KEY} chars)"
          [ -z "$NEXTAUTH_SECRET" ] && echo "❌ NEXTAUTH_SECRET: MISSING" && MISSING=1 || {
            if [ ${#NEXTAUTH_SECRET} -lt 32 ]; then
              echo "❌ NEXTAUTH_SECRET: TOO SHORT (${#NEXTAUTH_SECRET} < 32 chars)"
              MISSING=1
            else
              echo "✅ NEXTAUTH_SECRET: SET (${#NEXTAUTH_SECRET} chars)"
            fi
          }
          [ -z "$DATABASE_URL" ] && echo "❌ DATABASE_URL: MISSING" && MISSING=1 || echo "✅ DATABASE_URL: SET"
          
          # Check optional keys (OAuth removed - no longer required)
          [ -z "$OPENAI_API_KEY" ] && echo "⚠️  OPENAI_API_KEY: NOT SET (optional)" || echo "✅ OPENAI_API_KEY: SET"
          [ -z "$GEMINI_API_KEY" ] && echo "⚠️  GEMINI_API_KEY: NOT SET (optional)" || echo "✅ GEMINI_API_KEY: SET"
          
          echo ""
          if [ $MISSING -eq 1 ]; then
            echo "════════════════════════════════════════════════════════════"
            echo "❌ DEPLOYMENT BLOCKED: Missing secrets!"
            echo "════════════════════════════════════════════════════════════"
            echo ""
            echo "📋 SETUP INSTRUCTIONS:"
            echo "1. Go to: https://github.com/Anamsayyed016/Naukrimili/settings/secrets/actions"
            echo "2. Add these secrets:"
            echo ""
            echo "   HOST = srv1054971.hstgr.cloud"
            echo "   SSH_USER = root"
            echo "   SSH_PORT = 22"
            echo "   SSH_KEY = (paste complete private key)"
            echo "   NEXTAUTH_SECRET = (32+ random characters)"
            echo "   DATABASE_URL = postgresql://user:pass@host:5432/db"
            echo ""
            echo "   Note: Google OAuth removed - GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET no longer required"
            echo ""
            exit 1
          fi
          
          echo "✅ ALL SECRETS VALIDATED"
          echo "════════════════════════════════════════════════════════════"

  build:
    name: "🔨 Build Application"
    runs-on: ubuntu-latest
    needs: validate_secrets
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: |
          set -e
          echo "📦 Installing dependencies..."
          
          # Configure npm
          npm config set registry https://registry.npmjs.org/
          npm config set progress false
          npm config set audit false
          npm config set fund false
          npm config set legacy-peer-deps true
          
          # Clean npm cache to avoid stale data
          npm cache clean --force 2>/dev/null || true
          
          # Check for package-lock.json version mismatches
          echo "🔍 Checking for lockfile issues..."
          if grep -q '"@prisma/client".*"7\.' package-lock.json 2>/dev/null && grep -q '"@prisma/client".*"6\.18\.0"' package.json 2>/dev/null; then
            echo "⚠️  Detected Prisma version mismatch in lockfile"
            echo "📝 Lockfile has @prisma/client@7.x but package.json requires 6.18.0"
            echo "🔄 Using npm install to resolve version conflicts..."
            rm -rf node_modules package-lock.json 2>/dev/null || true
            npm install --omit=dev --legacy-peer-deps --ignore-scripts 2>&1 || {
              echo "❌ npm install failed, trying with force..."
              npm install --omit=dev --legacy-peer-deps --force --ignore-scripts 2>&1 || {
                echo "❌ Installation failed"
                echo "📋 Last 20 lines of npm error:"
                tail -20
                exit 1
              }
            }
            echo "✅ npm install succeeded (lockfile regenerated)"
          else
            # Try npm ci first (faster, more reliable if lockfile is valid)
            # If it fails, fall back to npm install
            echo "🔧 Attempting npm ci..."
            if npm ci --omit=dev --ignore-scripts 2>&1; then
              echo "✅ npm ci succeeded"
            else
              echo "⚠️  npm ci failed, trying npm install..."
              # Remove node_modules if partial install exists
              rm -rf node_modules 2>/dev/null || true
              # Use npm install with legacy-peer-deps
              npm install --omit=dev --legacy-peer-deps --ignore-scripts 2>&1 || {
                echo "❌ npm install failed, trying with force..."
                npm install --omit=dev --legacy-peer-deps --force --ignore-scripts 2>&1 || {
                  echo "❌ All installation methods failed"
                  echo "📋 Last 20 lines of npm error:"
                  npm install --omit=dev --legacy-peer-deps --force --ignore-scripts 2>&1 | tail -20
                  exit 1
                }
              }
              echo "✅ npm install succeeded"
            fi
          fi
          
          # Verify installation
          [ -d node_modules ] || { echo "❌ node_modules directory not found"; exit 1; }
          
          # Check for critical packages
          [ -d node_modules/next ] || { echo "❌ Next.js not installed"; exit 1; }
          [ -d node_modules/react ] || { echo "❌ React not installed"; exit 1; }
          
          echo "✅ Dependencies installed successfully"

      - name: Generate Prisma Client
        env:
          SKIP_DB_VALIDATION: true
          SKIP_POSTINSTALL: true
        run: |
          set -e
          echo "🔧 Generating Prisma Client..."
          [ -f prisma/schema.prisma ] || { echo "❌ schema.prisma not found"; exit 1; }
          
          # Clean any existing Prisma client
          rm -rf node_modules/.prisma 2>/dev/null || true
          rm -rf node_modules/@prisma/client 2>/dev/null || true
          
          # Check if Prisma CLI is available, install if needed
          if ! command -v prisma &> /dev/null && [ ! -f node_modules/.bin/prisma ]; then
            echo "📦 Prisma CLI not found, installing temporarily..."
            npm install --no-save prisma@6.18.0 --legacy-peer-deps 2>&1 | tail -10 || {
              echo "⚠️  Failed to install Prisma CLI, trying with npx..."
            }
          fi
          
          # Generate Prisma client
          echo "⚙️  Running prisma generate..."
          npx prisma@6.18.0 generate --schema=prisma/schema.prisma 2>&1 | tail -20 || {
            echo "❌ Prisma generate failed"
            echo "📋 Checking Prisma installation..."
            ls -la node_modules/.bin/prisma 2>/dev/null || echo "Prisma CLI not in node_modules/.bin"
            exit 1
          }
          
          # Verify Prisma client was generated
          if [ -d node_modules/.prisma/client ] || [ -d node_modules/@prisma/client ]; then
            echo "✅ Prisma client generated successfully"
          else
            echo "❌ Prisma client not found after generation"
            echo "📋 Checking node_modules structure..."
            ls -la node_modules/.prisma 2>/dev/null || echo "No .prisma directory"
            ls -la node_modules/@prisma 2>/dev/null || echo "No @prisma directory"
            exit 1
          fi

      - name: Build Next.js
        timeout-minutes: 12
        env:
          NODE_ENV: production
          NEXTAUTH_SECRET: ${{ env.NEXTAUTH_SECRET }}
          NEXTAUTH_URL: ${{ env.PROD_URL }}
          NEXT_PUBLIC_APP_URL: ${{ env.PROD_URL }}
          DATABASE_URL: "postgresql://ci:ci@127.0.0.1:5432/ci"
          NEXT_TELEMETRY_DISABLED: 1
          SKIP_ENV_VALIDATION: 1
          SKIP_DB_VALIDATION: true
          NODE_OPTIONS: --max-old-space-size=6144
          ESLINT_NO_DEV_ERRORS: true
          OPENAI_API_KEY: ${{ env.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ env.GEMINI_API_KEY }}
        run: |
          echo "🔨 Building Next.js..."
          npm run build 2>&1 | tail -30 || {
            echo "⚠️  npm run build failed, trying npx next build..."
            npx next build 2>&1 | tail -30 || { echo "❌ Build failed"; exit 1; }
          }
          
          [ -d .next ] || { echo "❌ .next not created"; exit 1; }
          FILE_COUNT=$(find .next -type f 2>/dev/null | wc -l)
          [ "$FILE_COUNT" -gt 50 ] || { echo "❌ .next appears empty ($FILE_COUNT files)"; exit 1; }
          
          # Verify critical build artifacts exist
          echo "🔍 Verifying build artifacts..."
          CRITICAL_FILES=(
            ".next/server/middleware-manifest.json"
            ".next/required-server-files.json"
            ".next/BUILD_ID"
          )
          
          MISSING_FILES=0
          for file in "${CRITICAL_FILES[@]}"; do
            if [ ! -f "$file" ]; then
              echo "❌ Missing critical file: $file"
              MISSING_FILES=$((MISSING_FILES + 1))
            else
              echo "✅ Found: $file"
            fi
          done
          
          # Verify routes are built (check for route manifest)
          if [ -f ".next/routes-manifest.json" ]; then
            echo "✅ Routes manifest found"
            ROUTE_COUNT=$(cat .next/routes-manifest.json 2>/dev/null | grep -o '"pages":\[' | wc -l || echo "0")
            echo "📋 Routes detected in manifest"
          fi
          
          # Check for static and dynamic routes
          if [ -d ".next/server/app" ]; then
            APP_ROUTES=$(find .next/server/app -name "page.js" -o -name "route.js" 2>/dev/null | wc -l)
            echo "✅ Found $APP_ROUTES app routes"
          fi
          
          if [ -d ".next/server/pages" ]; then
            PAGE_ROUTES=$(find .next/server/pages -name "*.js" 2>/dev/null | wc -l)
            echo "✅ Found $PAGE_ROUTES page routes"
          fi
          
          if [ "$MISSING_FILES" -gt 0 ]; then
            echo "❌ Build verification failed: $MISSING_FILES critical files missing"
            exit 1
          fi
          
          echo "✅ Build successful ($FILE_COUNT files, all critical artifacts present)"

      - name: Create deployment bundle
        run: |
          set -e
          echo "📦 Creating deployment bundle..."
          
          # Step 1: Ensure all file operations from build are complete
          echo "⏳ Waiting for all file operations to complete..."
          sync  # Flush all pending writes to disk
          sleep 3  # Give extra time for any background processes
          sync  # Flush again after wait
          
          # Step 2: Verify .next directory is stable (no recent modifications)
          echo "🔍 Verifying .next directory is stable..."
          if [ -f .next/BUILD_ID ]; then
            BUILD_ID_TIME=$(stat -c %Y .next/BUILD_ID 2>/dev/null || stat -f %m .next/BUILD_ID 2>/dev/null || echo "0")
            CURRENT_TIME=$(date +%s)
            TIME_DIFF=$((CURRENT_TIME - BUILD_ID_TIME))
            if [ "$TIME_DIFF" -lt 5 ]; then
              echo "⚠️  BUILD_ID was recently modified, waiting..."
              sleep 5
              sync
            fi
          fi
          
          # Step 3: Create isolated bundle directory in /tmp (completely separate from workspace)
          BUNDLE_DIR="/tmp/deploy-bundle-$$-$(date +%s)"
          mkdir -p "$BUNDLE_DIR"
          echo "📁 Bundle directory: $BUNDLE_DIR"
          
          # Step 4: Copy all required files to bundle directory (creates snapshot)
          echo "📋 Copying files to bundle directory..."
          
          # Copy .next directory with all contents
          echo "  → Copying .next directory..."
          cp -r .next "$BUNDLE_DIR/" || { echo "❌ Failed to copy .next"; exit 1; }
          
          # Remove source maps to reduce bundle size (after copy, so original is untouched)
          echo "  → Removing source maps..."
          find "$BUNDLE_DIR/.next" -name "*.map" -type f -delete 2>/dev/null || true
          
          # Copy configuration files
          echo "  → Copying configuration files..."
          [ -f package.json ] && cp package.json "$BUNDLE_DIR/" || { echo "❌ package.json not found"; exit 1; }
          [ -f package-lock.json ] && cp package-lock.json "$BUNDLE_DIR/" || echo "⚠️  package-lock.json not found (will be regenerated)"
          [ -f ecosystem.config.cjs ] && cp ecosystem.config.cjs "$BUNDLE_DIR/" || echo "⚠️  ecosystem.config.cjs not found"
          [ -f next.config.mjs ] && cp next.config.mjs "$BUNDLE_DIR/" || { echo "❌ next.config.mjs not found"; exit 1; }
          
          # Copy directories
          echo "  → Copying directories..."
          [ -d public ] && cp -r public "$BUNDLE_DIR/" 2>/dev/null || echo "⚠️  public directory not found"
          [ -d prisma ] && cp -r prisma "$BUNDLE_DIR/" 2>/dev/null || { echo "❌ prisma directory not found"; exit 1; }
          
          # Step 5: Verify bundle directory has all required content
          echo "🔍 Verifying bundle contents..."
          [ -d "$BUNDLE_DIR/.next" ] || { echo "❌ .next directory not in bundle"; exit 1; }
          [ -f "$BUNDLE_DIR/package.json" ] || { echo "❌ package.json not in bundle"; exit 1; }
          [ -f "$BUNDLE_DIR/next.config.mjs" ] || { echo "❌ next.config.mjs not in bundle"; exit 1; }
          [ -d "$BUNDLE_DIR/prisma" ] || { echo "❌ prisma directory not in bundle"; exit 1; }
          
          # Step 6: Ensure all copies are flushed to disk
          echo "💾 Flushing file system writes..."
          sync
          sleep 1
          
          # Step 7: Create tar archive from isolated snapshot directory
          # Use --warning=no-file-changed to suppress warnings (files shouldn't change in isolated dir)
          # But still fail on actual errors
          echo "🗜️  Creating tar archive from snapshot..."
          cd "$BUNDLE_DIR"
          
          # Count files before tar
          FILE_COUNT=$(find . -type f | wc -l)
          echo "  → Archiving $FILE_COUNT files..."
          
          # Create tar with error handling
          if tar --warning=no-file-changed -czf release.tar.gz . 2>&1; then
            echo "✅ Tar archive created successfully"
          else
            TAR_EXIT=$?
            # Check if it's just a warning (exit code 1) or real error (other codes)
            if [ "$TAR_EXIT" -eq 1 ]; then
              echo "⚠️  Tar reported file changes (likely false positive in isolated dir)"
              # Verify tar file was still created
              if [ -f release.tar.gz ]; then
                echo "✅ Tar file created despite warning"
              else
                echo "❌ Tar file not created"
                exit 1
              fi
            else
              echo "❌ Tar failed with exit code $TAR_EXIT"
              exit 1
            fi
          fi
          
          # Verify tar file integrity
          if [ ! -f release.tar.gz ]; then
            echo "❌ release.tar.gz not created"
            exit 1
          fi
          
          TAR_SIZE=$(du -h release.tar.gz | cut -f1)
          echo "  → Archive size: $TAR_SIZE"
          
          # Test tar file can be read
          if tar -tzf release.tar.gz > /dev/null 2>&1; then
            echo "✅ Tar archive is valid"
          else
            echo "❌ Tar archive is corrupted"
            exit 1
          fi
          
          cd - > /dev/null
          
          # Step 8: Move tar file to workspace root
          echo "📦 Moving bundle to workspace..."
          mv "$BUNDLE_DIR/release.tar.gz" . || { echo "❌ Failed to move release.tar.gz"; exit 1; }
          
          # Step 9: Clean up bundle directory
          echo "🧹 Cleaning up..."
          rm -rf "$BUNDLE_DIR"
          
          # Step 10: Final verification
          [ -f release.tar.gz ] || { echo "❌ release.tar.gz not found in workspace"; exit 1; }
          FINAL_SIZE=$(du -h release.tar.gz | cut -f1)
          echo "✅ Bundle created successfully: $FINAL_SIZE"

      - name: Upload bundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: deployment-bundle
          path: release.tar.gz
          retention-days: 1

  deploy:
    name: "🚀 Deploy to Production (Zero-Downtime)"
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download bundle
        uses: actions/download-artifact@v4
        with:
          name: deployment-bundle

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          
          # Handle both escaped and unescaped newlines
          echo "$SSH_KEY" | sed 's/\\n/\n/g' > ~/.ssh/deploy_key
          sed -i 's/\r$//' ~/.ssh/deploy_key  # Remove CRLF
          chmod 600 ~/.ssh/deploy_key
          
          # Validate SSH key
          grep -q "BEGIN.*PRIVATE" ~/.ssh/deploy_key || { echo "❌ Invalid SSH key"; exit 1; }
          grep -q "END.*PRIVATE" ~/.ssh/deploy_key || { echo "❌ SSH key missing END marker"; exit 1; }
          echo "✅ SSH key valid ($(wc -c < ~/.ssh/deploy_key) bytes)"

      - name: Test SSH connection
        run: |
          SSH_OPTS="-i ~/.ssh/deploy_key -p $SSH_PORT -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15 -o IdentitiesOnly=yes"
          ssh $SSH_OPTS "$SSH_USER@$HOST" "echo '✅ SSH OK' && uname -a" || {
            echo "❌ SSH failed - check SSH_KEY, HOST, SSH_PORT, SSH_USER"
            exit 1
          }

      - name: Upload bundle to staging
        run: |
          SSH_OPTS="-i ~/.ssh/deploy_key -p $SSH_PORT -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15 -o IdentitiesOnly=yes -o BatchMode=yes"
          
          # Create folders
          ssh $SSH_OPTS "$SSH_USER@$HOST" "mkdir -p $DEPLOY_FOLDER $STAGING_FOLDER $BACKUP_FOLDER"
          
          # Upload
          TIMESTAMP=$(date +%s)
          BUNDLE_NAME="release-$TIMESTAMP.tar.gz"
          rsync -avz -e "ssh $SSH_OPTS" release.tar.gz "$SSH_USER@$HOST:$STAGING_FOLDER/$BUNDLE_NAME" || {
            echo "❌ rsync failed"
            exit 1
          }
          echo "✅ Bundle uploaded to $STAGING_FOLDER/$BUNDLE_NAME"
          
          # Verify upload succeeded by checking file exists and has reasonable size
          echo "🔍 Verifying upload..."
          REMOTE_SIZE=$(ssh $SSH_OPTS "$SSH_USER@$HOST" "stat -f%z $STAGING_FOLDER/$BUNDLE_NAME 2>/dev/null || stat -c%s $STAGING_FOLDER/$BUNDLE_NAME 2>/dev/null || echo '0'")
          LOCAL_SIZE=$(stat -f%z release.tar.gz 2>/dev/null || stat -c%s release.tar.gz 2>/dev/null || echo '0')
          
          if [ "$REMOTE_SIZE" -eq 0 ] || [ "$REMOTE_SIZE" -lt 1000000 ]; then
            echo "❌ Upload verification failed: Remote file size is $REMOTE_SIZE bytes (expected > 1MB)"
            exit 1
          fi
          
          # Allow 5% size difference for compression/transfer differences
          SIZE_DIFF=$((REMOTE_SIZE - LOCAL_SIZE))
          SIZE_DIFF_PERCENT=$((SIZE_DIFF * 100 / LOCAL_SIZE))
          if [ ${SIZE_DIFF_PERCENT#-} -gt 5 ]; then
            echo "⚠️  Size mismatch: Local=$LOCAL_SIZE, Remote=$REMOTE_SIZE (${SIZE_DIFF_PERCENT}% difference)"
            echo "⚠️  Continuing anyway, but this may indicate a partial upload"
          else
            echo "✅ Upload verified: $REMOTE_SIZE bytes (matches local: $LOCAL_SIZE bytes)"
          fi
          
          # Save bundle name for deploy step (both locally and remotely)
          echo "$BUNDLE_NAME" > bundle_name.txt
          [ -f bundle_name.txt ] && [ -s bundle_name.txt ] || {
            echo "❌ Failed to save bundle name locally"
            exit 1
          }
          echo "✅ Bundle name saved locally: $(cat bundle_name.txt)"
          
          # Also save to remote server for the deployment script with verification
          echo "📝 Saving bundle name to remote server..."
          echo "$BUNDLE_NAME" | ssh $SSH_OPTS "$SSH_USER@$HOST" "mkdir -p /tmp && cat > /tmp/bundle_name.txt && chmod 644 /tmp/bundle_name.txt" || {
            echo "❌ Failed to save bundle name to remote server"
            echo "📋 Attempting to diagnose..."
            ssh $SSH_OPTS "$SSH_USER@$HOST" "ls -la /tmp/bundle_name.txt 2>/dev/null || echo 'File does not exist'; echo 'Bundle name should be: $BUNDLE_NAME'"
            exit 1
          }
          
          # Verify the file was written correctly on remote server
          echo "🔍 Verifying bundle name was saved on remote server..."
          REMOTE_BUNDLE_NAME_CHECK=$(ssh $SSH_OPTS "$SSH_USER@$HOST" "cat /tmp/bundle_name.txt 2>/dev/null || echo ''")
          if [ -z "$REMOTE_BUNDLE_NAME_CHECK" ] || [ "$REMOTE_BUNDLE_NAME_CHECK" != "$BUNDLE_NAME" ]; then
            echo "❌ Bundle name verification failed on remote server"
            echo "   Expected: $BUNDLE_NAME"
            echo "   Got: $REMOTE_BUNDLE_NAME_CHECK"
            echo "📋 Checking remote file..."
            ssh $SSH_OPTS "$SSH_USER@$HOST" "ls -la /tmp/bundle_name.txt && cat /tmp/bundle_name.txt || echo 'File not found'"
            exit 1
          fi
          
          echo "✅ Bundle name verified on remote server: $REMOTE_BUNDLE_NAME_CHECK"

      - name: Save bundle name
        uses: actions/upload-artifact@v4
        with:
          name: bundle-info
          path: bundle_name.txt
          retention-days: 1

  finalize:
    name: "✅ Finalize Deployment (Zero-Downtime Swap)"
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - name: Download bundle info
        uses: actions/download-artifact@v4
        with:
          name: bundle-info

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "$SSH_KEY" | sed 's/\\n/\n/g' > ~/.ssh/deploy_key
          sed -i 's/\r$//' ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

      - name: Deploy with zero-downtime swap
        run: |
          set -e
          
          # Step 1: Read bundle name from artifact (most reliable source)
          echo "📦 Reading bundle name from artifact..."
          if [ ! -f bundle_name.txt ]; then
            echo "❌ bundle_name.txt artifact not found"
            echo "📋 Current directory contents:"
            ls -la
            exit 1
          fi
          
          LOCAL_BUNDLE_NAME=$(cat bundle_name.txt | tr -d '\n\r ' | head -c 200)
          
          # Verify bundle name was read correctly
          if [ -z "$LOCAL_BUNDLE_NAME" ]; then
            echo "❌ bundle_name.txt is empty"
            echo "📋 File contents:"
            cat bundle_name.txt | od -c || cat bundle_name.txt
            echo "📋 File size: $(stat -f%z bundle_name.txt 2>/dev/null || stat -c%s bundle_name.txt 2>/dev/null || echo 'unknown') bytes"
            exit 1
          fi
          
          # Verify bundle name format
          if ! echo "$LOCAL_BUNDLE_NAME" | grep -qE '^release-[0-9]+\.tar\.gz$'; then
            echo "⚠️  Bundle name format unexpected: $LOCAL_BUNDLE_NAME"
            echo "⚠️  Expected format: release-<timestamp>.tar.gz"
            echo "⚠️  Continuing anyway..."
          else
            echo "✅ Bundle name format valid: $LOCAL_BUNDLE_NAME"
          fi
          
          SSH_OPTS="-i ~/.ssh/deploy_key -p $SSH_PORT -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15 -o IdentitiesOnly=yes"
          
          echo "🚀 Deploying $LOCAL_BUNDLE_NAME with zero-downtime swap..."
          
          # Step 2: Verify bundle was uploaded before proceeding
          echo "🔍 Verifying bundle exists on remote server..."
          ssh $SSH_OPTS "$SSH_USER@$HOST" "test -f $STAGING_FOLDER/$LOCAL_BUNDLE_NAME" || {
            echo "❌ Bundle verification failed: $STAGING_FOLDER/$LOCAL_BUNDLE_NAME"
            echo "📋 Checking staging folder contents..."
            ssh $SSH_OPTS "$SSH_USER@$HOST" "ls -lah $STAGING_FOLDER 2>/dev/null | head -20 || echo 'Folder does not exist'"
            exit 1
          }
          echo "✅ Bundle verified on remote server"
          
          # Step 3: Optionally verify /tmp/bundle_name.txt exists and matches (for debugging)
          echo "🔍 Checking /tmp/bundle_name.txt on remote server (optional verification)..."
          REMOTE_SAVED_NAME=$(ssh $SSH_OPTS "$SSH_USER@$HOST" "cat /tmp/bundle_name.txt 2>/dev/null | tr -d '\n\r ' || echo ''")
          if [ -n "$REMOTE_SAVED_NAME" ] && [ "$REMOTE_SAVED_NAME" = "$LOCAL_BUNDLE_NAME" ]; then
            echo "✅ Remote bundle name file matches: $REMOTE_SAVED_NAME"
          else
            if [ -z "$REMOTE_SAVED_NAME" ]; then
              echo "⚠️  /tmp/bundle_name.txt is missing or empty on remote server"
              echo "   This is OK - we'll use LOCAL_BUNDLE_NAME from artifact instead"
            else
              echo "⚠️  Remote bundle name mismatch (non-critical)"
              echo "   Local (from artifact): $LOCAL_BUNDLE_NAME"
              echo "   Remote (/tmp/bundle_name.txt): $REMOTE_SAVED_NAME"
              echo "   Will use LOCAL_BUNDLE_NAME as primary source"
            fi
          fi
          
          # Step 4: Ensure /tmp/bundle_name.txt exists on remote (for the heredoc script)
          echo "📝 Ensuring /tmp/bundle_name.txt exists on remote server..."
          echo "$LOCAL_BUNDLE_NAME" | ssh $SSH_OPTS "$SSH_USER@$HOST" "mkdir -p /tmp && cat > /tmp/bundle_name.txt && chmod 644 /tmp/bundle_name.txt" || {
            echo "⚠️  Failed to write /tmp/bundle_name.txt on remote (non-critical, will use LOCAL_BUNDLE_NAME)"
          }
          
          # Verify it was written
          REMOTE_VERIFY=$(ssh $SSH_OPTS "$SSH_USER@$HOST" "cat /tmp/bundle_name.txt 2>/dev/null | tr -d '\n\r ' || echo ''")
          if [ "$REMOTE_VERIFY" = "$LOCAL_BUNDLE_NAME" ]; then
            echo "✅ /tmp/bundle_name.txt written successfully on remote server"
          else
            echo "⚠️  /tmp/bundle_name.txt verification failed (non-critical)"
            echo "   Will use LOCAL_BUNDLE_NAME passed via heredoc instead"
          fi
          
          # Remote deployment script
          # NOTE: Using unquoted EOF so that $STAGING_FOLDER, $DEPLOY_FOLDER, $LOCAL_BUNDLE_NAME, etc. 
          # are expanded on the GitHub Actions runner before being sent to the remote server.
          # This prevents paths like "/release-123.tar.gz" caused by empty variables on the remote side.
          # LOCAL_BUNDLE_NAME is the PRIMARY source (from artifact), most reliable.
          ssh $SSH_OPTS "$SSH_USER@$HOST" << EOF
          set -e
          
          # PRIMARY: Use LOCAL_BUNDLE_NAME passed from GitHub Actions (most reliable)
          # This comes from the artifact and is guaranteed to be set
          REMOTE_BUNDLE_NAME="$LOCAL_BUNDLE_NAME"
          echo "✅ Using bundle name from GitHub Actions artifact: $REMOTE_BUNDLE_NAME"
          
          # SECONDARY: Verify /tmp/bundle_name.txt matches (for consistency check)
          if [ -f /tmp/bundle_name.txt ] && [ -s /tmp/bundle_name.txt ]; then
            REMOTE_FILE_NAME=$(cat /tmp/bundle_name.txt | tr -d '\n\r ' | head -c 200)
            if [ -n "$REMOTE_FILE_NAME" ] && [ "$REMOTE_FILE_NAME" = "$REMOTE_BUNDLE_NAME" ]; then
              echo "✅ /tmp/bundle_name.txt matches: $REMOTE_FILE_NAME"
            else
              echo "⚠️  /tmp/bundle_name.txt mismatch (using LOCAL_BUNDLE_NAME instead)"
              echo "   From artifact: $REMOTE_BUNDLE_NAME"
              echo "   From /tmp/bundle_name.txt: ${REMOTE_FILE_NAME:-'(empty)'}"
            fi
          else
            echo "⚠️  /tmp/bundle_name.txt not found or empty (using LOCAL_BUNDLE_NAME)"
          fi
          
          # TERTIARY: Final fallback - search staging folder (only if LOCAL_BUNDLE_NAME is somehow empty)
          if [ -z "$REMOTE_BUNDLE_NAME" ]; then
            echo "⚠️  LOCAL_BUNDLE_NAME is empty, searching staging folder as last resort..."
            if [ -d "$STAGING_FOLDER" ]; then
              BUNDLE_PATH=$(ls -t "$STAGING_FOLDER"/*.tar.gz 2>/dev/null | head -1)
              
              if [ -n "$BUNDLE_PATH" ] && [ -f "$BUNDLE_PATH" ]; then
                REMOTE_BUNDLE_NAME=$(basename "$BUNDLE_PATH")
                echo "✅ Found bundle in staging folder: $REMOTE_BUNDLE_NAME"
              else
                echo "❌ No .tar.gz files found in staging folder: $STAGING_FOLDER"
                echo "📋 Staging folder contents:"
                ls -lah "$STAGING_FOLDER" 2>/dev/null || echo "  (folder does not exist)"
                exit 1
              fi
            else
              echo "❌ Staging folder does not exist: $STAGING_FOLDER"
              exit 1
            fi
          fi
          
          # Final verification: bundle name must not be empty
          if [ -z "$REMOTE_BUNDLE_NAME" ]; then
            echo "❌ CRITICAL: Bundle name is still empty after all attempts"
            echo "📋 Debug information:"
            echo "   LOCAL_BUNDLE_NAME from GitHub Actions: $LOCAL_BUNDLE_NAME"
            echo "   /tmp/bundle_name.txt exists: $([ -f /tmp/bundle_name.txt ] && echo 'yes' || echo 'no')"
            echo "   /tmp/bundle_name.txt content: $(cat /tmp/bundle_name.txt 2>/dev/null || echo 'empty')"
            echo "   /tmp/bundle_name.txt size: $(stat -c%s /tmp/bundle_name.txt 2>/dev/null || echo '0') bytes"
            echo "   Staging folder exists: $([ -d "$STAGING_FOLDER" ] && echo 'yes' || echo 'no')"
            echo "   Staging folder contents:"
            ls -lah "$STAGING_FOLDER" 2>/dev/null | head -10 || echo "    (does not exist)"
            exit 1
          fi
          
          # Verify bundle name format (should be release-*.tar.gz)
          if ! echo "$REMOTE_BUNDLE_NAME" | grep -qE '^release-[0-9]+\.tar\.gz$'; then
            echo "⚠️  Bundle name format unexpected: $REMOTE_BUNDLE_NAME"
            echo "⚠️  Expected format: release-<timestamp>.tar.gz"
            echo "⚠️  Continuing anyway..."
          fi
          
          STAGING_PATH="$STAGING_FOLDER/$REMOTE_BUNDLE_NAME"
          
          # Verify bundle file exists and is readable
          if [ ! -f "$STAGING_PATH" ]; then
            echo "❌ Bundle file not found: $STAGING_PATH"
            echo "📋 Available files in staging folder:"
            ls -lah "$STAGING_FOLDER" 2>/dev/null || echo "  (folder does not exist)"
            echo "📋 Looking for files matching pattern:"
            ls -lah "$STAGING_FOLDER"/release-*.tar.gz 2>/dev/null || echo "  (no matching files)"
            exit 1
          fi
          
          # Verify bundle file is not empty
          BUNDLE_SIZE=$(stat -f%z "$STAGING_PATH" 2>/dev/null || stat -c%s "$STAGING_PATH" 2>/dev/null || echo "0")
          if [ "$BUNDLE_SIZE" -lt 1000000 ]; then
            echo "❌ Bundle file is too small: $BUNDLE_SIZE bytes (expected > 1MB)"
            exit 1
          fi
          
          echo "✅ Bundle verified: $REMOTE_BUNDLE_NAME ($(du -h "$STAGING_PATH" | cut -f1), $BUNDLE_SIZE bytes)"
          TEMP_DEPLOY="/var/www/naukrimili-temp-$$"
          PROD_DEPLOY="$DEPLOY_FOLDER"
          BACKUP_PATH="$BACKUP_FOLDER/backup-$(date +%s)"
          
          echo "📦 Bundle: $REMOTE_BUNDLE_NAME"
          echo "📂 Temp folder: $TEMP_DEPLOY"
          
          # Extract to temp folder
          mkdir -p "$TEMP_DEPLOY"
          cd "$TEMP_DEPLOY"
          tar -xzf "$STAGING_PATH" || { echo "❌ Extract failed"; exit 1; }
          echo "✅ Bundle extracted"
          
          # Install dependencies
          if [ -f package-lock.json ]; then
            echo "📚 Installing dependencies..."
            npm ci --omit=dev --legacy-peer-deps --prefer-offline 2>&1 | tail -10 || {
              echo "⚠️  npm ci failed, trying npm install..."
              npm install --omit=dev --legacy-peer-deps 2>&1 | tail -10 || {
                echo "❌ Dependency installation failed"
                exit 1
              }
            }
            echo "✅ Dependencies installed"
          else
            echo "⚠️  package-lock.json not found, skipping dependency installation"
          fi
          
          # Run migrations if schema changed
          if [ -f prisma/schema.prisma ]; then
            export NODE_ENV=production
            # DATABASE_URL is expanded from GitHub Actions env before heredoc
            export DATABASE_URL="$DATABASE_URL"
            echo "🗄️  Running migrations..."
            if timeout 60 npx prisma migrate deploy 2>&1; then
              echo "✅ Migrations completed"
            else
              MIGRATION_EXIT=$?
              echo "⚠️  Migration exited with code $MIGRATION_EXIT"
              # Don't fail deployment for migration warnings, but log them
              if [ "$MIGRATION_EXIT" -ne 0 ] && [ "$MIGRATION_EXIT" -ne 1 ]; then
                echo "❌ Migration failed critically"
                exit 1
              fi
            fi
          else
            echo "⚠️  prisma/schema.prisma not found, skipping migrations"
          fi
          
          # Start PM2 in temp folder to test
          echo "🧪 Testing application in temp folder..."
          pm2 delete jobportal-test 2>/dev/null || true
          if pm2 start ecosystem.config.cjs --name jobportal-test --env production; then
            sleep 3
            
            # Health check
            if curl -f -s http://localhost:3000/api/health > /dev/null 2>&1; then
              echo "✅ Health check passed"
              
              # Success: backup current and swap
              echo "🔄 Swapping to production..."
              pm2 delete jobportal-test 2>/dev/null || true
              
              if [ -d "$PROD_DEPLOY/.next" ]; then
                echo "💾 Backing up current version..."
                mkdir -p "$BACKUP_PATH"
                cp -r "$PROD_DEPLOY/.next" "$BACKUP_PATH/" || true
                cp -r "$PROD_DEPLOY/node_modules" "$BACKUP_PATH/" 2>/dev/null || true
              fi
              
              # Atomic swap
              rm -rf "$PROD_DEPLOY.old"
              [ -d "$PROD_DEPLOY" ] && mv "$PROD_DEPLOY" "$PROD_DEPLOY.old" || true
              mv "$TEMP_DEPLOY" "$PROD_DEPLOY"
              rm -rf "$PROD_DEPLOY.old" "$TEMP_DEPLOY"
              
              # Start production PM2
              cd "$PROD_DEPLOY"
              pm2 delete jobportal 2>/dev/null || true
              pm2 start ecosystem.config.cjs --env production || {
                echo "❌ PM2 failed, rolling back..."
                [ -d "$BACKUP_PATH/.next" ] && {
                  cp -r "$BACKUP_PATH/.next" "$PROD_DEPLOY/"
                  pm2 start ecosystem.config.cjs --env production
                }
                exit 1
              }
              pm2 save --force
              sleep 2
              
              # Final health check
              if curl -f -s http://localhost:3000/api/health > /dev/null 2>&1; then
                echo "✅ Production deployment successful"
                # Clean old backups (keep last 3)
                cd "$BACKUP_FOLDER"
                ls -t | tail -n +4 | xargs -r rm -rf
                rm -f "$STAGING_PATH"
              else
                echo "❌ Production health check failed"
                exit 1
              fi
            else
              echo "❌ Health check failed in temp folder"
              pm2 logs jobportal-test --lines 20 || true
              rm -rf "$TEMP_DEPLOY"
              exit 1
            fi
          else
            echo "❌ Failed to start PM2 in temp folder"
            pm2 logs jobportal-test --lines 50 || true
            rm -rf "$TEMP_DEPLOY"
            exit 1
          fi
          EOF
          
          echo "✅ Deployment complete - zero downtime achieved!"

      - name: Post-deployment verification
        run: |
          SSH_OPTS="-i ~/.ssh/deploy_key -p $SSH_PORT -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15"
          
          echo "🧪 Running post-deployment checks..."
          
          # Check PM2 status
          echo ""
          echo "📊 PM2 Status:"
          ssh $SSH_OPTS "$SSH_USER@$HOST" "pm2 status" || echo "⚠️  Could not get PM2 status"
          
          # Check health endpoint
          echo ""
          echo "🏥 Health Check:"
          ssh $SSH_OPTS "$SSH_USER@$HOST" "curl -s http://localhost:3000/api/health | head -c 200 || echo '⚠️  No response'" || true
          
          # Check disk usage
          echo ""
          echo "💾 Disk Usage:"
          ssh $SSH_OPTS "$SSH_USER@$HOST" "du -sh $DEPLOY_FOLDER $BACKUP_FOLDER 2>/dev/null || echo 'N/A'" || true
          
          echo ""
          echo "✅ Post-deployment verification complete"
