name: 🚀 Password SSH Deployment

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: 🚀 Deploy via SSH
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: 69.62.73.84
        username: root
        key: |
          -----BEGIN OPENSSH PRIVATE KEY-----
          b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAFwAAAAdzc2gtcn
          NhAAAAAwEAAQAAAQEA2K8xK8xK8xK8xK8xK8xK8xK8xK8xK8xK8xK8xK8xK8xK8xK8xK8x
          -----END OPENSSH PRIVATE KEY-----
        port: 22
        script: |
          cd /var/www/jobportal || mkdir -p /var/www/jobportal && cd /var/www/jobportal
          if [ ! -d ".git" ]; then
            git clone https://github.com/Anamsayyed016/Naukrimili.git .
          else
            git pull origin main
          fi
          npm install --force
          npm run build
          pm2 restart jobportal || pm2 start ecosystem.config.cjs --env production